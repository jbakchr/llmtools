[project]
name = "llmtools"
version = "0.1.0"
dependencies = [
    "requests>=2.30.0"
]


[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}